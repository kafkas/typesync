models:
  Project:
    fields:
      id:
        type: string
      name:
        type: string
      completed:
        type: boolean
      order:
        type: int
      createdBy:
        type: string
        optional: true
