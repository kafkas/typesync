import { None, Str, Undefined } from './_types.js';

export const UNDEFINED: Undefined = { type: 'undefined' };

export const NONE: None = { type: 'none' };

export const STRING: Str = { type: 'str' };
