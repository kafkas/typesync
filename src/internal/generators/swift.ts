import { GenerationOutput } from '../GenerationOutput';
import { type Schema } from '../Schema';

export class SwiftGenerator {
  public async generate(schema: Schema): Promise<GenerationOutput> {
    // TODO: Implement
    return new GenerationOutput();
  }
}
